# Council Arena Configuration
# All settings are optional - the app works out of the box for local use

# ============================================================
# GitHub Copilot (No Additional Configuration Required!)
# ============================================================
# Council Arena uses your GitHub OAuth login to access Copilot.
# Simply log in with a GitHub account that has an active Copilot subscription.
# Available AI models are fetched dynamically based on your subscription.

# ============================================================
# Server Configuration
# ============================================================
# Data Directory (where session secret and database are stored)
# DATA_DIR=./data

# Server port
# PORT=8080

# Environment (development/production)
# ENV=development

# Log level (debug, info, warn, error)
# LOG_LEVEL=info

# ============================================================
# OAuth Configuration (Optional)
# ============================================================
# Override built-in OAuth credentials (optional, for custom OAuth app)
# GITHUB_CLIENT_ID=your_github_oauth_client_id
# GITHUB_CLIENT_SECRET=your_github_oauth_client_secret
